# Project Context for Memory Store - Loaded by hooks, used by Claude for intelligent enrichment
# Hooks provide foundational metadata, Claude adds conversational context (HYBRID APPROACH)

PROJECT_NAME="mem-plugin"
PROJECT_FULL_NAME="Memory Store Plugin for Claude Code"
PROJECT_TYPE="Claude Code Plugin"
PROJECT_PURPOSE="Persistent memory across sessions via autonomous tracking"

# Current Version & Focus
VERSION="1.2.3"
VERSION_FOCUS="Hybrid context: Hooks provide metadata, Claude adds intelligence"

# Architecture (for Claude's understanding)
ARCHITECTURE="Queue-based producer-consumer"
ARCHITECTURE_KEY="Hooks write .memory-queue.jsonl → memory-queue-processor skill enriches context → MCP stores"

# Component Directory Structure (for file location context)
COMPONENTS="hooks/ scripts/ skills/ commands/"

# Repository
REPO_URL="https://github.com/julep-ai/memory-store-plugin"

# Note for Claude: Read this file to understand project structure, then add conversational
# context based on what user is actually working on. Don't just dump all metadata blindly.
